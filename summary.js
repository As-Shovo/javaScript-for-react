/* 
Fundamentals
1.1 variable (let, const) three types variable
1.2 condition (<, >, ===, !==, <=, >=,) && || , if else else-if
1.3 array: declare, length, index, push, pop, indexOf,includes
1.4 for loop, while loop
1.5 function return parameter
1.6 Object property, including array, object

-------------------------------

es-6
1. template string ${}
2. spread (...)
2.1 copy an array then add a new element to an array
2.2 use filter to remove an element from an array
3. Arrow function
3.1 no parameter ()
3.2 single parameters
3.3 multiple parameters ()
3.4 multi line
4. destructuring : object destructuring array destructuring
5. object shorthand
6. function default parameter default value == 'something'
7. Optional chaining .?
--------------------------
Browser API

1. local storage session storage
2. localtion API
3. History API
4. fetch

------------------------------
Others
1. array: map, forEach, filter, find
2. ternary operator
3. logical and logical of && ||
4. JSON (stringify , parse)

*/

// short Hand
const x = 50;
const y = 100;
const obj = {
    x:x,
    y:y
};

const name = 'Solimullah';
const area = 'Dhaka';

const nobab = {
    name = name,
    area = area
}

// shortcut

const nobab2 = {name,area}
